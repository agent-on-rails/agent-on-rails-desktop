{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "windows-updater",
  "description": "Tauri updater permissions (Windows only)",
  "platforms": ["windows"],
  "windows": ["main"],
  "permissions": [
    "updater:default"
  ]
}
